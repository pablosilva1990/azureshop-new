# No Azure App Service, PORT é injetado automaticamente pela plataforma.
PORT=3000
APP_ENV=development
DB_PROVIDER=sqlite
# No Azure App Service, aponte para um caminho persistente (ex.: /home/data/loja.db),
# pois o disco padrão do contêiner é efêmero entre reinícios/deploys.
SQLITE_PATH=./data/loja.db
# Para Azure SQL, use DB_PROVIDER=sqlserver e configure:
# AZURE_SQL_SERVER=servidor.database.windows.net
# AZURE_SQL_DATABASE=imersao
# AZURE_SQL_USER=usuario
# AZURE_SQL_PASSWORD=senha
# Em produção, prefira identidade gerenciada: AZURE_SQL_AUTH=managedIdentity
AI_ENABLED=false
# AZURE_OPENAI_ENDPOINT=https://seu-recurso.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=gpt-4.1-mini
